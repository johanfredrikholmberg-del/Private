# Styles

Gemensamma visuella regler ligger i `app.css`. Planerarspecifika regler ligger i `planner.css`.

När en sida får mycket egen styling ska den brytas ut till sidans egen stylesheet i `pages/<sida>/` och laddas via bootstrap/index i stället för att växa i den gemensamma filen.

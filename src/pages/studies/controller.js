(()=>{'use strict';
const root=window.StudieLotsV2=window.StudieLotsV2||{},ctx=root.appContext;if(!ctx)throw new Error('StudieLots app context saknas');
const {state,q,qa,fmt,esc}=ctx;
function source(kind){state.source=kind;qa('[data-source]').forEach(b=>b.classList.toggle('active',b.dataset.source===kind));q('#sourceCopy').textContent=kind==='ladok'?'Ladda upp ett nationellt resultatintyg från Ladok.':'Antagning.se-import kopplas in i nästa steg.';q('#uploadBtn').disabled=kind!=='ladok';q('#uploadBtn').style.opacity=kind==='ladok'?'1':'.55'}
function setCourses(courses,label){state.courses=courses||[];try{sessionStorage.setItem('studielots_v2_courses',JSON.stringify(state.courses))}catch(_){}const box=q('#uploadState');if(box){box.style.display='block';box.innerHTML=`<b>${esc(label||'Meriter hämtade')}</b><span>${state.courses.length} kurser · ${fmt(state.courses.reduce((s,c)=>s+Number(c.hp||0),0))} hp</span>`}root.navigation?.show?.('opportunities')}
function bind(){q('#uploadBtn')?.addEventListener('click',()=>q('#fileInput')?.click());q('#fileInput')?.addEventListener('change',async e=>{const f=e.target.files?.[0];if(!f)return;const box=q('#uploadState');if(box){box.style.display='block';box.innerHTML='<b>Läser meriter…</b><span>Kurser, nivå och examen analyseras.</span>'}try{const d=await root.ladok.importPdf(f);setCourses(d.courses,d.sourceLabel||'Ladok-meriter')}catch(err){if(box)box.innerHTML=`<b>Kunde inte läsa intyget</b><span>${esc(err.message||'Prova ett nationellt resultatintyg från Ladok.')}</span>`}});qa('[data-source]').forEach(b=>b.addEventListener('click',()=>source(b.dataset.source)));window.addEventListener('studielots:v2-demo',e=>setCourses(e.detail.courses,'Exempeldata · 150 hp'));source('ladok')}
root.studies={source,setCourses,bind};
})();
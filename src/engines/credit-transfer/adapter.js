(()=>{'use strict';const root=window.StudieLotsEngines=window.StudieLotsEngines||{};const engine=()=>root.creditTransfer;function assess(source,target,options={}){return engine()?.assess?.(source,target,options)||null}function applyToRows(rows,merits,options={}){return(Array.isArray(rows)?rows:[]).map(row=>{let best=null;for(const merit of(Array.isArray(merits)?merits:[])){const result=assess(merit,row,options);if(!result)continue;const rank=result.classification==='strong'?3:result.classification==='relevant'?2:1;if(!best||rank>best.rank)best={rank,result,merit}}return best?{...row,creditTransfer:best.result,creditTransferCountsInStudyPlan:best.result.countsInStudyPlan,creditTransferMatchedCourse:best.merit?.name||best.merit?.title||'',creditTransferMatchedCourseCode:best.merit?.code||best.merit?.courseCode||''}:{...row,creditTransfer:null}})}function summary(rows){const a=(Array.isArray(rows)?rows:[]).filter(r=>r.creditTransfer),strong=a.filter(r=>r.creditTransferCountsInStudyPlan),hp=r=>Number(r?.hp??r?.credits??r?.ects??0)||0;return Object.freeze({strongCount:strong.length,strongHp:strong.reduce((s,r)=>s+hp(r),0),relevantCount:a.filter(r=>r.creditTransfer?.classification==='relevant').length,limitedCount:a.filter(r=>r.creditTransfer?.classification==='limited').length})}root.creditTransferAdapter=Object.freeze({assess,applyToRows,summary});})();
name: Umea structure pass
on:
  push:
    branches: [umu-program-import]
    paths:
      - 'scripts/umu-direct-pass.mjs'
      - '.github/workflows/umu-structure-pass.yml'
permissions:
  contents: write
jobs:
  enrich:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: umu-program-import
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Read Umea programme structures directly
        run: node scripts/umu-direct-pass.mjs
      - name: Validate structures
        run: node -e "const fs=require('fs');const m=JSON.parse(fs.readFileSync('data/umu/meta.json'));if(!m.directParsed)process.exit(1);console.log(m.counts)"
      - name: Save structures
        run: |
          git config user.name studielots-bot
          git config user.email actions@users.noreply.github.com
          git add -f data/umu
          git diff --cached --quiet || git commit -m 'Import Umea programme structures directly [skip ci]'
          git pull --rebase origin umu-program-import
          git push origin HEAD:umu-program-import

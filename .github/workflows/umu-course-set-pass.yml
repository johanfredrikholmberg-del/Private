name: Umea course set pass
on:
  push:
    branches: [umu-program-import]
    paths:
      - 'scripts/umu-course-set-pass.mjs'
      - '.github/workflows/umu-course-set-pass.yml'
permissions:
  contents: write
jobs:
  enrich:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: umu-program-import
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Parse official Umea course sets
        run: node scripts/umu-course-set-pass.mjs
      - name: Validate structures
        run: node -e "const fs=require('fs');const m=JSON.parse(fs.readFileSync('data/umu/meta.json'));const s=JSON.parse(fs.readFileSync('data/umu/structures.json'));if(s.length!==m.programmes)process.exit(1);console.log(m.counts)"
      - name: Save improvements
        run: |
          git config user.name studielots-bot
          git config user.email actions@users.noreply.github.com
          git add -f data/umu
          git diff --cached --quiet || git commit -m 'Add Umea official course sets [skip ci]'
          git pull --rebase origin umu-program-import
          git push origin HEAD:umu-program-import

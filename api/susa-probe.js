export default async function handler(req,res){try{const url='https://api.skolverket.se/susa-navet/susa-navet-emil3.yaml';const r=await fetch(url,{headers:{accept:'text/yaml,text/plain'}});const yaml=await r.text();const lines=yaml.split(/\r?\n/);const hits=[];for(let i=0;i<lines.length;i++){if(/UH_Subject|subject|codeList|codes|vocabulary/i.test(lines[i])){hits.push({line:i+1,text:lines.slice(Math.max(0,i-3),Math.min(lines.length,i+8))});if(hits.length>=80)break}}const routes=lines.filter(x=>/^\s{2}\/[^:]+:\s*$/.test(x)).map(x=>x.trim().replace(/:$/,''));return res.status(200).json({routes,hits})}catch(e){return res.status(500).json({error:String(e)})}}

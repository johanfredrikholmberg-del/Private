(()=>{'use strict';
const clean=v=>String(v??'').replace(/\s+/g,' ').trim(),num=v=>Number.isFinite(Number(v))?Number(v):0;
const code=v=>clean(v?.code||v?.courseCode).toUpperCase(),hp=v=>num(v?.hp||v?.credits),key=v=>code(v).replace(/[^A-Z0-9ÅÄÖ]/g,'');
const remaining=r=>Math.max(0,hp(r)-Math.max(0,Math.min(hp(r),num(r?.creditedHp)||(r?.credited?hp(r):0))));
const prereqs=r=>(Array.isArray(r?.prerequisiteCodes)?r.prerequisiteCodes:[]).map(x=>clean(x).toUpperCase()).filter(Boolean);
const alternatives=r=>(Array.isArray(r?.alternatives)?r.alternatives:[]).filter(a=>a?.verified===true||a?.equivalent===true||a?.fulfillsRequirement===true||a?.accepted===true);
const requirement=r=>clean(r?.requirement||r?.requirementLabel||r?.requirementName||r?.name||r?.code||'Examenskrav');
const institution=o=>clean(o?.university||o?.institution||o?.provider||o?.organizer||o?.school).toLocaleLowerCase('sv-SE');
const selectedUniversity=()=>clean(document.querySelector('#planner .planner-top div span')?.textContent);
const sameUniversity=(o,u)=>{const a=institution(o),b=clean(u).toLocaleLowerCase('sv-SE');return !!a&&!!b&&(a===b||a.includes(b)||b.includes(a))};
const allowedOffering=(o,u)=>o?.distance===true||sameUniversity(o,u);
function semester(date){const d=new Date(date);if(!Number.isFinite(d.getTime()))return null;const m=d.getMonth()+1,y=d.getFullYear();if(m>=6&&m<=8)return{kind:'summer',year:y,key:y*10+1.5,label:`Sommar ${y}`};return{kind:m<=6?'spring':'autumn',year:y,key:y*10+(m<=6?1:2),label:`${m<=6?'Vår':'Höst'} ${y}`}}
function currentSemester(){const d=new Date(),m=d.getMonth()+1,y=d.getFullYear();return y*10+(m<=6?1:2)}
function eligible(list,{summer=true,university=''}={}){university=clean(university)||selectedUniversity();const now=Date.now()-86400000,current=currentSemester();return(list||[]).filter(o=>{const t=Date.parse(o.startDate||o.start||o.from);return Number.isFinite(t)&&t>=now&&(summer||o.__semester?.kind!=='summer')&&allowedOffering(o,university)&&Number(o.__semester?.key)>=current}).sort((a,b)=>a.__semester.key-b.__semester.key||(sameUniversity(b,university)-sameUniversity(a,university))||(b.distance===true)-(a.distance===true)||num(b.pace)-num(a.pace))}
function bestOffering(list,opts={}){return eligible(list,opts)[0]||null}
async function offerings(rows){const codes=[...new Set(rows.flatMap(r=>[code(r),...alternatives(r).map(code)]).filter(Boolean))],map=new Map();for(let i=0;i<codes.length;i+=20){try{const qs=new URLSearchParams({codes:codes.slice(i,i+20).join(','),type:'course',standaloneOnly:'true'}),res=await fetch('/api/susa-offerings?'+qs),data=await res.json();for(const o of data?.offerings||[]){if(o?.standaloneSearchable!==true||!o?.url)continue;const s=semester(o.startDate||o.start||o.from);if(!s)continue;const k=clean(o.code).toUpperCase().replace(/[^A-Z0-9ÅÄÖ]/g,'');if(!map.has(k))map.set(k,[]);map.get(k).push({...o,__semester:s})}}catch(_){}}return map}
function slots(count=16){const d=new Date(),out=[];let y=d.getFullYear(),kind=d.getMonth()+1<=6?'spring':'autumn';for(let i=0;i<count;i++){out.push({kind,year:y,key:y*10+(kind==='spring'?1:2),label:`${kind==='spring'?'Vår':'Höst'} ${y}`,rows:[],hp:0});if(kind==='spring')kind='autumn';else{kind='spring';y++}}return out}
function storedMeritCodes(){try{return new Set(JSON.parse(sessionStorage.getItem('studielots_v2_courses')||'[]').map(code).filter(Boolean))}catch(_){return new Set()}}
function completedCodes(rows){const out=storedMeritCodes();for(const r of rows)if(r?.credited===true||num(r?.creditedHp)>0){if(code(r))out.add(code(r))}return out}
function nominalKey(row,regular,firstTerm){const i=Math.max(0,(num(row?.term)||firstTerm)-firstTerm);return regular[i]?.key||null}
async function build(rows,{maxHp=30,summer=true,university=''}={}){university=clean(university)||selectedUniversity();maxHp=[30,37.5,45,60].includes(Number(maxHp))?Number(maxHp):30;const regular=slots(),firstTerm=Math.min(...rows.map(r=>num(r?.term)||1),1),active=rows.filter(r=>remaining(r)>.01).sort((a,b)=>prereqs(a).length-prereqs(b).length||(num(a.term)||99)-(num(b.term)||99)),map=await offerings(active),summerTerms=new Map(),scheduled=new Map(),doneCodes=completedCodes(rows),suggestions=[],unscheduled=[];if(!active.length)return{terms:[],suggestions:[],unscheduled:[],maxHp,summer,remainingHp:0,offeringCount:0,distanceCount:0,university,accelerationCount:0};let offeringCount=0,distanceCount=0,accelerationCount=0;
for(const original of active){const req=prereqs(original),nominal=nominalKey(original,regular,firstTerm);let choices=[{course:original,offers:eligible(map.get(key(original)),{summer,university}),alt:false},...alternatives(original).map(a=>({course:{...original,code:a.code||original.code,name:a.name||original.name},offers:eligible(map.get(key(a)),{summer,university}),alt:true}))].filter(x=>x.offers.length);if(!choices.length){unscheduled.push({row:original,reason:'Inget verifierbart framtida kurstillfälle'});continue}choices.sort((a,b)=>a.offers[0].__semester.key-b.offers[0].__semester.key||(sameUniversity(b.offers[0],university)-sameUniversity(a.offers[0],university))||(a.alt-b.alt));let selected=null;
for(const choice of choices){for(const offer of choice.offers){const prereqReady=req.every(c=>doneCodes.has(c)||doneCodes.has(c.replace(/[^A-Z0-9ÅÄÖ]/g,''))||((scheduled.get(c)||scheduled.get(c.replace(/[^A-Z0-9ÅÄÖ]/g,'')))<offer.__semester.key));if(req.length&&!prereqReady)continue;const amount=remaining(original);if(offer.__semester.kind==='summer'){if(!summer)continue;let t=summerTerms.get(offer.__semester.key);if(!t)t={...offer.__semester,rows:[],hp:0};if(t.hp+amount>maxHp+.001)continue;selected={choice,offer,target:t,isSummer:true};break}const t=regular.find(x=>x.key===offer.__semester.key);if(!t||t.hp+amount>maxHp+.001)continue;selected={choice,offer,target:t,isSummer:false};break}if(selected)break}
if(!selected){unscheduled.push({row:original,reason:'Kurstillfälle finns men kan inte placeras med behörighet/studietakt'});continue}const {choice,offer,target,isSummer}=selected,chosen=choice.course,reasons=[];if(choice.alt)reasons.push('Verifierat alternativ');const local=sameUniversity(offer,university);reasons.push(local?'Samma lärosäte':'Distans');if(offer.distance===true&&local)reasons.push('Distans');if(offer.pace)reasons.push(`${num(offer.pace)} % studietakt`);if(isSummer)reasons.push('Sommarkurs');if(nominal&&offer.__semester.key<nominal){reasons.push('Tidigare än ordinarie programtermin');accelerationCount++}if(maxHp>30&&target.hp+remaining(original)>30)reasons.push('Ökad studietakt');const scheduledRow={...chosen,hp:remaining(original),__offer:offer,__requirement:requirement(original),__reasons:[...reasons,'Täcker examenskrav']};target.rows.push(scheduledRow);target.hp+=remaining(original);if(isSummer)summerTerms.set(target.key,target);for(const c of [code(chosen),code(original),key(chosen),key(original)].filter(Boolean))scheduled.set(c,offer.__semester.key);offeringCount++;if(offer.distance===true)distanceCount++;suggestions.push({requirement:requirement(original),requiredHp:remaining(original),courseCode:code(chosen),courseName:chosen.name||original.name,offer,verified:true,reasons:[...reasons],originalTerm:num(original.term)||1,nominalKey:nominal,accelerates:Boolean(nominal&&offer.__semester.key<nominal)||isSummer||choice.alt})}
const terms=[...regular.filter(x=>x.rows.length),...summerTerms.values()].sort((a,b)=>a.key-b.key);return{terms,suggestions:suggestions.sort((a,b)=>a.offer.__semester.key-b.offer.__semester.key),unscheduled,maxHp,summer,remainingHp:active.reduce((s,r)=>s+remaining(r),0),scheduledHp:suggestions.reduce((s,x)=>s+x.requiredHp,0),offeringCount,distanceCount,university,accelerationCount,alternativeCount:suggestions.filter(x=>x.reasons.includes('Verifierat alternativ')).length,summerCount:suggestions.filter(x=>x.reasons.includes('Sommarkurs')).length}}
window.StudieLotsV2=window.StudieLotsV2||{};window.StudieLotsV2.fast=Object.freeze({build,remaining,bestOffering,allowedOffering});
})();
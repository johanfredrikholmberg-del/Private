(()=>{'use strict';
const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
const hp=s=>Number(clean(s).replace(',','.').match(/\d+(?:\.\d+)?/)?.[0]||0);
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
function remainingHp(){const t=document.querySelector('#planner .planner-progress')?.textContent||'';const m=t.match(/([\d,.]+)\s*hp\s+återstår\s+enligt\s+examensmatchningen/i);return m?hp(m[1]):0}
function maxHp(){const a=document.querySelector('#fastPlan [data-max].active');return a?hp(a.textContent):30}
function candidates(){return[...document.querySelectorAll('#ordinaryPlan .course')].map((el,i)=>{const status=clean(el.querySelector('.status')?.textContent);if(/kan räknas in/i.test(status))return null;const b=el.querySelector('.course-main b'),code=clean(el.querySelector('.course-main small')?.textContent),credits=hp(el.querySelector('.course-main strong')?.textContent);return{name:clean(b?.textContent)||'Programkurs',code,credits:credits||0,order:i}}).filter(Boolean)}
function choose(total){let left=total;const out=[];for(const c of candidates()){if(left<=.01)break;const take=Math.min(c.credits||left,left);if(take<=0)continue;out.push({...c,credits:take});left-=take}return{rows:out,uncovered:Math.max(0,left)}}
function periods(rows,cap){const periods=[];for(const c of rows){let p=periods.at(-1);if(!p||p.hp+c.credits>cap+.01){p={hp:0,rows:[]};periods.push(p)}p.rows.push(c);p.hp+=c.credits}return periods}
function render(){const fast=document.querySelector('#fastPlan');if(!fast||fast.hidden)return;const total=remainingHp();if(total<=0)return;const old=fast.querySelector('.fast-course-proposal');old?.remove();const verified=fast.querySelector('.term-list .offer-link');if(verified)return;const picked=choose(total),ps=periods(picked.rows,maxHp());if(!ps.length)return;const sec=document.createElement('section');sec.className='fast-course-proposal';sec.innerHTML=`<div class="proposal-head"><b>Förslag till upplägg</b><span>Utgår från ${total.toLocaleString('sv-SE')} hp som återstår i examensmatchningen. Kurserna hämtas från den valda programplanen; placering och tillgodoräknande måste verifieras innan upplägget är definitivt.</span></div><div class="proposal-periods">${ps.map((p,i)=>`<article class="proposal-period"><div class="proposal-period-head"><div><b>${i===0?'Nästa studieperiod':`Studieperiod ${i+1}`}</b><span>${i===0?'Prioriterade återstående krav':'Fortsatt upplägg'}</span></div><strong>${p.hp.toLocaleString('sv-SE')} hp</strong></div>${p.rows.map(c=>`<div class="proposal-course"><div>${c.code?`<small>${esc(c.code)}</small>`:''}<b>${esc(c.name)}</b><span>Programkurs · kurstillfälle ej verifierat</span></div><strong>${c.credits.toLocaleString('sv-SE')} hp</strong></div>`).join('')}</article>`).join('')}</div>${picked.uncovered>.01?`<div class="info-note"><b>${picked.uncovered.toLocaleString('sv-SE')} hp behöver kompletteras</b><span>Programplanen innehåller ännu inte tillräckligt detaljerade kurskrav för att föreslå resten utan att gissa.</span></div>`:''}`;fast.appendChild(sec)}
function install(){const s=document.createElement('style');s.textContent='.fast-course-proposal{display:grid;gap:12px;margin:14px 0 24px}.proposal-head{display:grid;gap:6px;padding:16px 18px;background:#e5f1eb;border-radius:18px;color:#0b302b}.proposal-head span{font-size:14px;line-height:1.45}.proposal-periods{display:grid;gap:10px}.proposal-period{background:#fff;border:1px solid rgba(0,63,54,.1);border-radius:20px;overflow:hidden}.proposal-period-head,.proposal-course{display:flex;justify-content:space-between;gap:14px;padding:15px 18px;align-items:center}.proposal-period-head{background:rgba(0,63,54,.035)}.proposal-period-head div,.proposal-course div{display:grid;gap:3px}.proposal-period-head span,.proposal-course span,.proposal-course small{font-size:12px;color:#6b7571}.proposal-course{border-top:1px solid rgba(0,63,54,.08)}.proposal-period strong,.proposal-course strong{color:#07584c;white-space:nowrap}';document.head.appendChild(s);new MutationObserver(()=>requestAnimationFrame(render)).observe(document.querySelector('#fastPlan')||document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden','class']});document.addEventListener('click',e=>{if(e.target.closest('[data-max],[data-summer],[data-route="fast"]'))setTimeout(render,100)},true);setTimeout(render,400)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();